import React from "react";
import { PageContainer } from "../../styles";

type Props = {};

const Likes = (props: Props) => {
	return <PageContainer>Likes</PageContainer>;
};

export default Likes;
